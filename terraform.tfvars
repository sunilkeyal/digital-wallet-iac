# Variables are configured via TF_VAR_* environment variables.
# For local development, set values here or use TF_VAR_* env vars.
# Required vars: tenancy_ocid, compartment_ocid, region, ssh_public_key, jwt_secret, app_admin_password
